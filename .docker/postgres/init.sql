-- PostgreSQL initialization script
-- Create additional databases if needed
-- ALTER USER laravel CREATEDB;

-- Set timezone
SET timezone = 'UTC';

-- Enable extensions if needed
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp"; 